baseURL = "https://mohandacherir.github.io/Qdiv7/"
defaultContentLanguage = "en"
# Automatically generate robots.txt
enableRobotsTXT = true
languageCode = "en"
theme = "gokarna"
title = "Qdiv7"

[menu]
  [[menu.main]]
    # Display name
    name = "Posts"

    # Relative URL slug (appended to baseURL)
    url = "/posts/"

    # Lower weights are listed first in the menu (leftmost); higher weights are
    # listed last in the menu (rightmost)
    weight = 1

  [[menu.main]]
    name = "Tags"
    url = "/tags/"
    weight = 2
    
  [[menu.main]]
    # Unique identifiers are required for menu entries without a name property,
    # or for menu entries which re-use a name
    identifier = "github"

    # Absolute URL to external resource
    url = "https://github.com/MohandAcherir"
    weight = 3
    
    # Surround the menu entry (or name) with HTML content, such as Feather
    # icons: https://feathericons.com
    pre = "<span data-feather='github'></span>"
    post = ""


 [params]
    description = "My InfoSec blog"
    socialIcons = [
      {name = "twitter", url = "https://x.com/AcherirMoe/"},
      {name = "linkedin", url = "https://www.linkedin.com/in/mohand-arezki-acherir-b42911264"},
      {name = "rootme", url = "https://root-me.org/Acherir7"},
    ]
    avatarURL = "../images/0074.png"
    logo = "../images/0072.png"
	
